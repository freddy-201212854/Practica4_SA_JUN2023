module.exports = {
  testEnvironment: 'node',
  testMatch: ['**/*.test.js'], // Patrón para los archivos de prueba
  collectCoverage: true, // Habilita la recolección de cobertura de código
  coverageDirectory: 'coverage', // Directorio donde se generará el informe de cobertura
};